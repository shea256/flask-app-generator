from [[APP_NAME]] import app

# Put core elements here

"""
# database

from mongoengine import connect
from flask.ext.mongoengine import MongoEngine

connect(app.config['MONGODB_DB'],
	host='mongodb://' + app.config['DB_USERNAME'] + ':' + \
	app.config['DB_PASSWORD'] + '@' + app.config['DB_HOST_ADDRESS'])

db = MongoEngine(app)

"""
